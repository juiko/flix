<% if current_client.try(:admin?) %>
  <%= render 'partials/admin_dropdown' %>
<% end %>

<% if client_signed_in? %>
  <li class="dropdown">
    <a href="#"
       class="dropdown-toggle"
       data-toggle="dropdown"
       role="button"
       aria-haspopup="true"
       aria-expanded="false">
      Contenido
      <span class="caret">
      </span>
    </a>
    <ul class="dropdown-menu">
      <li><%= link_to 'Todo', root_url %></li>
      <li><%= link_to 'Series', shows_url %></li>
      <li><%= link_to 'Peliculas', movies_url %></li>
    </ul>
<% end %>


  </li>


  <% if client_signed_in? %>
    <li>
      <%= link_to 'Buscar', search_content_path %>
    </li>
  <% end %>
